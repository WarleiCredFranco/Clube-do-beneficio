import React from 'react';
import ReactDOM from 'react-dom';
import App from '../../resources/js/app'; // Importe o componente React principal aqui

const element = <App />; // Substitua 'App' pelo nome do seu componente principal

ReactDOM.render(element, document.getElementById('react-app'));
